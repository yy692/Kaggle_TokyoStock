1. Create a directory 'input' under home directory
2. Create a directory 'working' under home directory
3. Download the dataset (zipfile) from https://www.kaggle.com/competitions/jpx-tokyo-stock-exchange-prediction/data into input
4. Unzip the dataset
5. Run main.ipynb
